node_modules/
dist/

# Spacefast CLI state and the ignore file it writes beside the published build.
.spacefast/
.spacefast/state.json
/.spacefast-*
.spacefast/.spacefast-*
.spacefast/.space-link-*.tmp

# The Gutenberg issue draft, held until the cc handles are decided. Not published.
issue-draft.md
